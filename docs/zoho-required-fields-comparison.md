/**
 * ZOHO 必填字段对比报告
 * 生成时间: 2026-04-23
 */

## ZOHO 端实际必填字段（通过 API 测试 + metadata 查询）

只有 **1 个**：
- `Name` (主题)

测试结果：仅传 `{ Name: "测试" }` 即可成功创建记录。

---

## 本地 field-map.json 标记的必填字段

共 **18 个**（业务规则，非 ZOHO 强制）：

1. 渠道名称 (field50) - lookup
2. 文件存放路径 (field76)
3. 公证主体中文名 (field6)
4. 业务细类 (field67) - lookup
5. 1公证书使用地 (field229)
6. 9附件数量 (field25)
7. 10订单金额 (field20)
8. 业务员 (field8)
9. 订单所有者 (Owner) - 系统字段
10. 主题 (Name) - **唯一与 ZOHO 端一致的必填**
11. 订单日期 (field)
12. 供应商 (field78)
13. 文案 (field216)
14. 删除不负责证词 (field31)
15. 其他注意事项 (field36)
16. 需递交文件名 (field179)
17. 总页数 (field213)
18. 彩打页数 (field28)

---

## 结论

- **ZOHO 端宽松**：技术上只需要主题就能建单
- **本地严格**：我们在 value-transform.js 里强制校验 18 个字段，确保业务数据完整性
- **策略合理**：提前拦截不完整订单，避免垃圾数据进 ZOHO

如果未来 ZOHO 端增加必填字段，需要同步更新 field-map.json。
